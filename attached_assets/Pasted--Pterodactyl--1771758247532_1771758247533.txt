Ниже готовый запрос (промпт) для генерации биллинг-панели под Pterodactyl. Скопируйте его и отправьте в диалог с нейросетью (GPT, Claude и т.п.). Запрос составлен так, чтобы получить максимально полный ответ — от архитектуры до кода и инструкций.

---

**Запрос для ИИ:**

Ты — опытный fullstack-разработчик. Мне нужно создать полноценную биллинг-панель для игровой панели управления **Pterodactyl** с нуля. Панель должна быть самодостаточным веб-приложением, интегрированным с API Pterodactyl, и включать следующие возможности:

### 1. Основные модули
- **Управление серверами через API Pterodactyl**  
  Создание, удаление, приостановка, изменение ресурсов сервера. Автоматическая синхронизация статусов.
- **Каталог тарифов**  
  Возможность создавать и редактировать тарифные планы (CPU, RAM, диск, слотами, базами данных и т.д.). Цены, периоды оплаты (месяц, квартал, год).
- **Система заказов**  
  Пользователь выбирает тариф, оплачивает, сервер создаётся автоматически. Возможность продления/отмены подписки.
- **Платёжный шлюз**  
  Интеграция с **Stripe**, **PayPal** и **ЮKassa**. Настройка валют, цен через админку. Поддержка налогов (опционально).
- **Тикет-система**  
  Пользователи создают тикеты, прикрепляют файлы, общаются с поддержкой. Администраторы видят все тикеты и отвечают. Категории тикетов, статусы.
- **Админ-панель**  
  Управление пользователями (блокировка, смена ролей), тарифами, заказами, платежами, тикетами, логами действий. Все настройки (платежные системы, параметры интеграции с Pterodactyl, внешний вид) — через админку, без правки кода.
- **Личный кабинет пользователя**  
  Просмотр списка серверов, управление ими (если разрешено тарифом), история платежей, тикеты, настройки профиля.

### 2. Технические требования
- **Backend:** PHP 8.1+, фреймворк **Laravel** (последняя стабильная версия).
- **Frontend:** **Vue.js 3** (Composition API) + **Tailwind CSS** для красивого адаптивного дизайна. Тёмная/светлая тема.
- **База данных:** MySQL 5.7+ / MariaDB.
- **API Pterodactyl:** использовать официальный клиент (через Guzzle). Учитывать версию API Pterodactyl (1.x).
- **Безопасность:** защита от SQL-инъекций (через Eloquent), XSS, CSRF, правильное хеширование паролей, валидация ввода.
- **Документация:** подробная инструкция по установке (настройка окружения, миграции, заполнение .env, настройка планировщика, очередей).

### 3. Что должен содержать ответ
- Полная структура проекта (папки и ключевые файлы).
- Код основных компонентов:
  - Модели, миграции, контроллеры для пользователей, тарифов, заказов, платежей, тикетов.
  - Интеграция с платежными системами (сервисы/трейты).
  - Интеграция с Pterodactyl API (сервис для создания/управления серверами).
  - Vue-компоненты для фронтенда (основные страницы).
  - Админка на основе Laravel Filament / Custom Vue-панели (выбери оптимальный вариант).
- Конфигурационные файлы (например, config/pterodactyl.php, config/payments.php).
- Маршруты (web, api).
- Команды для artisan (очереди, синхронизация).
- Инструкция по развертыванию и настройке.

Учти, что панель должна быть готова к использованию после выполнения инструкций. Код должен быть модульным, легко расширяемым и читаемым. Приведи примеры для ключевых моментов: создание сервера через API после успешной оплаты, обработка вебхуков от платёжных систем, создание тикета.

Пожалуйста, предоставь ответ максимально подробно — как руководство для разработки, с объяснениями и готовыми фрагментами кода.

---

Этот запрос охватывает все важные аспекты и даст ИИ чёткое понимание задачи. Если нужно что‑то уточнить (например, предпочитаемый стек или детали интеграции), можно дополнить промпт перед отправкой.